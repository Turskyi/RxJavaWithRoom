<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tool="http://schemas.android.com/tools"
    style="@style/LLayoutForListItemStyle"
    android:weightSum="10"
    tool:ignore="UseCompoundDrawables">

    <ImageView
        android:id="@+id/ivFlag"
        style="@style/IViewStyle"
        android:contentDescription="@string/flag"
        tool:src="@android:drawable/ic_menu_gallery" />

    <!--    WebView is used in case of error while downloading image source from the internet. -->
    <WebView
        android:id="@+id/wvFlag"
        style="@style/IViewStyle"
        android:contentDescription="@string/flag"
        android:visibility="gone"
        tool:drawableStart="@android:drawable/ic_menu_gallery" />

    <TextView
        android:id="@+id/tvCountry"
        style="@style/TViewStyle"
        tool:text="@string/country" />

</LinearLayout>